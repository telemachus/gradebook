# TODO

+ Add validation.  Should the validation be part of the `LoadClass` function
  or separate?
+ Add functions to return emails, names, and numbers for a class.
+ Expand the `Student` struct so that it can store grades.
+ Add methods to the `Student` struct so that it can calculate grades.
